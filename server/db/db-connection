const { Pool } = require('pg');
const db = new Pool({
    //connectionString: process.env.DATABASE_URI
    connectionString: "postgresql://localhost/endangered_animals"
  });
//console.log("database test: " + process.env.DATABASE_URI)
  module.exports = db;